<section class="section">
  <div class="container">
    <div class="columns">
        <% if user_signed_in? %>
          <%= render 'tweets/profile' %>
        <% else %>
          <%= render 'tweets/trends' %>
        <% end %>
       <%= render 'tweets/feed' %>
       <%= render 'relationships/browse' %>
    </div>
  </div>
</section>